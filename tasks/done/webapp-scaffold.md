# Webapp — Scaffold Base

**Serviço:** webapp (BFF)  
**Responsável:** Caio Vieira  
**PR:** #4 (merged)  
**Status:** ✅ Concluído

## O que foi feito

Inicialização do projeto Nuxt 4 + Vue 3 na porta 3000:

- Estrutura base do projeto (`nuxt.config.ts`, `tsconfig.json`, ESLint)
- Layout padrão (`app/layouts/default.vue`)
- Página inicial (`app/pages/index.vue`)
- Página de login (`app/pages/login.vue`)
- `package.json` com dependências base

## O que ainda falta (ver tasks em todo/)

As páginas existentes são scaffolds sem funcionalidade real — não há chamadas à API, autenticação wired, nem rotas de BFF. As implementações de tela estão em `todo/`.
